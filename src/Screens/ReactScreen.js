import React from 'react'
import ReactProjects from '../ReactProjects';
import Nav from '../Nav';

const ReactScreen = () => {
  return (
    <div>
    <Nav />
      <ReactProjects />
    </div>
  )
}

export default ReactScreen;
